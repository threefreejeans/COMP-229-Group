import mongoose from 'mongoose';

interface MOptions {
    sketch?: boolean;
    forceAppendix?: boolean;
    scale?: number;
    center?: boolean;
    pad?: number;
}

declare const mongooseToErdMain: (modelNames: Array<string>, mongooseModel: typeof mongoose.model, options?: MOptions) => Promise<void>;

export { mongooseToErdMain };
